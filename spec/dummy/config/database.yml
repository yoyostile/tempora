defaults: &defaults
  adapter: postgresql
  pool: 15
  min_messages: warning

development:
  database: tempora_development
  host: localhost
  <<: *defaults

test:
  database: tempora_test
  host: localhost
  <<: *defaults
